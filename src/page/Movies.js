export const Movies = () => {
  return (
    <div>
      <form onSubmit={() => {}}>
        <input
          type="text"
          autoComplete="off"
          autoFocus
          placeholder="Search films"
          onChange={() => {}}
        />

        <button type="submit">
          <span>Search</span>
        </button>
      </form>
    </div>
  );
};
